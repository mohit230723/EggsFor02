import { WalletNode } from "./WalletNode";
import { AgentNode } from "./AgentNode";
import { SkillNode } from "./SkillNode";

export const nodeTypes = {
  wallet: WalletNode,
  agent: AgentNode,
  skill: SkillNode,
};

export { WalletNode, AgentNode, SkillNode };
