#pragma version 10
intcblock 1 8 0 64 16 32 352 360 256 320 336 392 456 464 472
bytecblock 0x 0x6164 0x7363 0x7066 0x0000000000000000 0x151f7c75 0x5f

// This TEAL was generated by TEALScript v0.107.2
// https://github.com/algorandfoundation/TEALScript

// This contract is compliant with and/or implements the following ARCs: [ ARC4 ]

// The following ten lines of TEAL handle initial program flow
// This pattern is used to make it easy for anyone to parse the start of the program and determine if a specific action is allowed
// Here, action refers to the OnComplete in combination with whether the app is being created or called
// Every possible action for this contract is represented in the switch statement
// If the action is not implemented in the contract, its respective branch will be "*NOT_IMPLEMENTED" which just contains "err"
txn ApplicationID
!
pushint 6
*
txn OnCompletion
+
switch *call_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *create_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED

*NOT_IMPLEMENTED:
	// The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?
	err

// createApplication()void
*abi_route_createApplication:
	// execute createApplication()void
	callsub createApplication
	intc 0 // 1
	return

// createApplication(): void
createApplication:
	proto 0 0

	// contracts\SkillMarketplace.algo.ts:35
	// this.skillCount.value = 0
	bytec 2 //  "sc"
	intc 2 // 0
	app_global_put

	// contracts\SkillMarketplace.algo.ts:36
	// this.platformFeeBps.value = 500
	bytec 3 //  "pf"
	pushint 500
	app_global_put

	// contracts\SkillMarketplace.algo.ts:37
	// this.admin.value = this.txn.sender
	bytec 1 //  "ad"
	txn Sender
	app_global_put
	retsub

// listSkill(pay,byte[64],byte[256],byte[16],byte[16],uint64,byte[64])uint64
*abi_route_listSkill:
	// The ABI return prefix
	bytec 5 // 0x151f7c75

	// ipcsCid: byte[64]
	txna ApplicationArgs 6
	dup
	len
	intc 3 // 64
	==

	// argument 0 (ipcsCid) for listSkill must be a byte[64]
	assert

	// price: uint64
	txna ApplicationArgs 5
	btoi

	// version: byte[16]
	txna ApplicationArgs 4
	dup
	len
	intc 4 // 16
	==

	// argument 2 (version) for listSkill must be a byte[16]
	assert

	// skillType: byte[16]
	txna ApplicationArgs 3
	dup
	len
	intc 4 // 16
	==

	// argument 3 (skillType) for listSkill must be a byte[16]
	assert

	// description: byte[256]
	txna ApplicationArgs 2
	dup
	len
	intc 8 // 256
	==

	// argument 4 (description) for listSkill must be a byte[256]
	assert

	// name: byte[64]
	txna ApplicationArgs 1
	dup
	len
	intc 3 // 64
	==

	// argument 5 (name) for listSkill must be a byte[64]
	assert

	// mbrPayment: pay
	txn GroupIndex
	intc 0 // 1
	-
	dup
	gtxns TypeEnum
	intc 0 //  pay
	==

	// argument 6 (mbrPayment) for listSkill must be a pay transaction
	assert

	// execute listSkill(pay,byte[64],byte[256],byte[16],byte[16],uint64,byte[64])uint64
	callsub listSkill
	itob
	concat
	log
	intc 0 // 1
	return

// listSkill(mbrPayment: PayTxn, name: StaticArray<byte, 64>, description: StaticArray<byte, 256>, skillType: StaticArray<byte, 16>, version: StaticArray<byte, 16>, price: uint64, ipcsCid: StaticArray<byte, 64>): uint64
//
// List a new skill on the marketplace.
// Requires an MBR payment for box storage.
listSkill:
	proto 7 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 2

	// contracts\SkillMarketplace.algo.ts:55
	// minMbr = 2500 + boxSize * 400
	pushint 194500
	frame_bury 0 // minMbr: uint64

	// contracts\SkillMarketplace.algo.ts:56
	// verifyPayTxn(mbrPayment, {
	//       receiver: this.app.address,
	//       amount: { greaterThanEqualTo: minMbr },
	//     })
	// verify receiver
	frame_dig -1 // mbrPayment: PayTxn
	gtxns Receiver
	global CurrentApplicationAddress
	==

	// transaction verification failed: {"txn":"mbrPayment","field":"receiver","expected":"this.app.address"}
	assert

	// verify amount
	frame_dig -1 // mbrPayment: PayTxn
	gtxns Amount
	frame_dig 0 // minMbr: uint64
	>=

	// transaction verification failed: {"txn":"mbrPayment","field":"amount","condition":"greaterThanEqualTo","expected":">=minMbr"}
	assert

	// contracts\SkillMarketplace.algo.ts:61
	// assert(price > 0, 'Price must be greater than 0')
	frame_dig -6 // price: uint64
	intc 2 // 0
	>

	// Price must be greater than 0
	assert

	// contracts\SkillMarketplace.algo.ts:65
	// skillId = this.skillCount.value + 1
	bytec 2 //  "sc"
	app_global_get
	intc 0 // 1
	+
	frame_bury 1 // skillId: uint64

	// contracts\SkillMarketplace.algo.ts:66
	// this.skillCount.value = skillId
	bytec 2 //  "sc"
	frame_dig 1 // skillId: uint64
	app_global_put

	// contracts\SkillMarketplace.algo.ts:69
	// skill: SkillMetadata = {
	//       name: name,
	//       description: description,
	//       skillType: skillType,
	//       version: version,
	//       price: price,
	//       seller: this.txn.sender,
	//       ipcsCid: ipcsCid,
	//       soldCount: 0,
	//       listedAt: globals.round,
	//       active: 1,
	//     }
	frame_dig -2 // name: StaticArray<byte, 64>
	frame_dig -3 // description: StaticArray<byte, 256>
	concat
	frame_dig -4 // skillType: StaticArray<byte, 16>
	concat
	frame_dig -5 // version: StaticArray<byte, 16>
	concat
	frame_dig -6 // price: uint64
	itob
	concat
	txn Sender
	concat
	frame_dig -7 // ipcsCid: StaticArray<byte, 64>
	concat
	bytec 4 // 0x0000000000000000
	concat
	global Round
	itob
	concat
	pushbytes 0x0000000000000001
	concat
	frame_bury 2 // skill: SkillMetadata

	// contracts\SkillMarketplace.algo.ts:82
	// assert(!this.skills(skillId).exists, 'Skill already exists with this ID')
	frame_dig 1 // skillId: uint64
	itob
	box_len
	swap
	pop
	!

	// Skill already exists with this ID
	assert

	// contracts\SkillMarketplace.algo.ts:84
	// this.skills(skillId).value = skill
	frame_dig 1 // skillId: uint64
	itob
	frame_dig 2 // skill: SkillMetadata
	box_put

	// contracts\SkillMarketplace.algo.ts:86
	// return skillId;
	frame_dig 1 // skillId: uint64

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 2
	retsub

// buySkill(pay,uint64)void
*abi_route_buySkill:
	// skillId: uint64
	txna ApplicationArgs 1
	btoi

	// payment: pay
	txn GroupIndex
	intc 0 // 1
	-
	dup
	gtxns TypeEnum
	intc 0 //  pay
	==

	// argument 1 (payment) for buySkill must be a pay transaction
	assert

	// execute buySkill(pay,uint64)void
	callsub buySkill
	intc 0 // 1
	return

// buySkill(payment: PayTxn, skillId: uint64): void
//
// Purchase a skill. Splits payment between seller and platform.
buySkill:
	proto 2 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 5

	// contracts\SkillMarketplace.algo.ts:96
	// assert(this.skills(skillId).exists, 'Skill not found')
	frame_dig -2 // skillId: uint64
	itob
	box_len
	swap
	pop

	// Skill not found
	assert

	// contracts\SkillMarketplace.algo.ts:97
	// skill = this.skills(skillId).value
	frame_dig -2 // skillId: uint64
	itob
	frame_bury 0 // storage key//skill

	// contracts\SkillMarketplace.algo.ts:98
	// assert(skill.active === 1, 'Skill is no longer active')
	intc 14 //  headOffset
	intc 1 // 8
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	intc 0 // 1
	==

	// Skill is no longer active
	assert

	// contracts\SkillMarketplace.algo.ts:99
	// assert(this.txn.sender !== skill.seller, 'Cannot buy your own skill')
	txn Sender
	intc 7 //  headOffset
	intc 5 // 32
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	!=

	// Cannot buy your own skill
	assert

	// contracts\SkillMarketplace.algo.ts:101
	// verifyPayTxn(payment, {
	//       receiver: this.app.address,
	//       amount: { greaterThanEqualTo: skill.price },
	//     })
	// verify receiver
	frame_dig -1 // payment: PayTxn
	gtxns Receiver
	global CurrentApplicationAddress
	==

	// transaction verification failed: {"txn":"payment","field":"receiver","expected":"this.app.address"}
	assert

	// verify amount
	frame_dig -1 // payment: PayTxn
	gtxns Amount
	intc 6 //  headOffset
	intc 1 // 8
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	>=

	// transaction verification failed: {"txn":"payment","field":"amount","condition":"greaterThanEqualTo","expected":">=skill.price"}
	assert

	// contracts\SkillMarketplace.algo.ts:107
	// purchaseKey = itob(skillId) + '_' + rawBytes(this.txn.sender)
	frame_dig -2 // skillId: uint64
	itob
	bytec 6 //  "_"
	concat
	txn Sender
	concat
	frame_bury 1 // purchaseKey: byte[]

	// contracts\SkillMarketplace.algo.ts:108
	// assert(!this.purchases(purchaseKey).exists, 'Already purchased this skill')
	frame_dig 1 // purchaseKey: byte[]
	dup
	len
	itob
	extract 6 2
	swap
	concat
	box_len
	swap
	pop
	!

	// Already purchased this skill
	assert

	// contracts\SkillMarketplace.algo.ts:111
	// platformFee = (skill.price * this.platformFeeBps.value) / 10000
	intc 6 //  headOffset
	intc 1 // 8
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	bytec 3 //  "pf"
	app_global_get
	*
	pushint 10000
	/
	frame_bury 2 // platformFee: uint64

	// contracts\SkillMarketplace.algo.ts:112
	// sellerAmount = skill.price - platformFee
	intc 6 //  headOffset
	intc 1 // 8
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	frame_dig 2 // platformFee: uint64
	-
	frame_bury 3 // sellerAmount: uint64

	// contracts\SkillMarketplace.algo.ts:115
	// sendPayment({
	//       receiver: skill.seller,
	//       amount: sellerAmount,
	//     })
	itxn_begin
	intc 0 //  pay
	itxn_field TypeEnum

	// contracts\SkillMarketplace.algo.ts:116
	// receiver: skill.seller
	intc 7 //  headOffset
	intc 5 // 32
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	itxn_field Receiver

	// contracts\SkillMarketplace.algo.ts:117
	// amount: sellerAmount
	frame_dig 3 // sellerAmount: uint64
	itxn_field Amount

	// Fee field not set, defaulting to 0
	intc 2 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// *if0_condition
	// contracts\SkillMarketplace.algo.ts:121
	// platformFee > 0
	frame_dig 2 // platformFee: uint64
	intc 2 // 0
	>
	bz *if0_end

	// *if0_consequent
	// contracts\SkillMarketplace.algo.ts:122
	// sendPayment({
	//         receiver: this.admin.value,
	//         amount: platformFee,
	//       })
	itxn_begin
	intc 0 //  pay
	itxn_field TypeEnum

	// contracts\SkillMarketplace.algo.ts:123
	// receiver: this.admin.value
	bytec 1 //  "ad"
	app_global_get
	itxn_field Receiver

	// contracts\SkillMarketplace.algo.ts:124
	// amount: platformFee
	frame_dig 2 // platformFee: uint64
	itxn_field Amount

	// Fee field not set, defaulting to 0
	intc 2 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

*if0_end:
	// contracts\SkillMarketplace.algo.ts:129
	// record: PurchaseRecord = {
	//       buyer: this.txn.sender,
	//       purchasedAt: globals.round,
	//       skillId: skillId,
	//     }
	txn Sender
	global Round
	itob
	concat
	frame_dig -2 // skillId: uint64
	itob
	concat
	frame_bury 4 // record: PurchaseRecord

	// contracts\SkillMarketplace.algo.ts:134
	// this.purchases(purchaseKey).value = record
	frame_dig 1 // purchaseKey: byte[]
	dup
	len
	itob
	extract 6 2
	swap
	concat
	frame_dig 4 // record: PurchaseRecord
	box_put

	// contracts\SkillMarketplace.algo.ts:137
	// updatedSkill: SkillMetadata = {
	//       name: skill.name,
	//       description: skill.description,
	//       skillType: skill.skillType,
	//       version: skill.version,
	//       price: skill.price,
	//       seller: skill.seller,
	//       ipcsCid: skill.ipcsCid,
	//       soldCount: skill.soldCount + 1,
	//       listedAt: skill.listedAt,
	//       active: skill.active,
	//     }
	intc 2 // 0
	intc 3 // 64
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	intc 3 //  headOffset
	intc 8 // 256
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 9 //  headOffset
	intc 4 // 16
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 10 //  headOffset
	intc 4 // 16
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 6 //  headOffset
	intc 1 // 8
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	itob
	concat
	intc 7 //  headOffset
	intc 5 // 32
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 11 //  headOffset
	intc 3 // 64
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 12 //  headOffset
	intc 1 // 8
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	intc 0 // 1
	+
	itob
	concat
	intc 13 //  headOffset
	intc 1 // 8
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	itob
	concat
	intc 14 //  headOffset
	intc 1 // 8
	frame_dig -2 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	itob
	concat
	frame_bury 5 // updatedSkill: SkillMetadata

	// contracts\SkillMarketplace.algo.ts:149
	// this.skills(skillId).value = updatedSkill
	frame_dig -2 // skillId: uint64
	itob
	frame_dig 5 // updatedSkill: SkillMetadata
	box_put
	retsub

// hasAccess(uint64,address)uint64
*abi_route_hasAccess:
	// The ABI return prefix
	bytec 5 // 0x151f7c75

	// buyer: address
	txna ApplicationArgs 2
	dup
	len
	intc 5 // 32
	==

	// argument 0 (buyer) for hasAccess must be a address
	assert

	// skillId: uint64
	txna ApplicationArgs 1
	btoi

	// execute hasAccess(uint64,address)uint64
	callsub hasAccess
	itob
	concat
	log
	intc 0 // 1
	return

// hasAccess(skillId: uint64, buyer: Address): uint64
//
// Check if a buyer has purchased a skill. Used by x402 content gate.
hasAccess:
	proto 2 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x

	// contracts\SkillMarketplace.algo.ts:156
	// purchaseKey = itob(skillId) + '_' + rawBytes(buyer)
	frame_dig -1 // skillId: uint64
	itob
	bytec 6 //  "_"
	concat
	frame_dig -2 // buyer: Address
	concat
	frame_bury 0 // purchaseKey: byte[]

	// contracts\SkillMarketplace.algo.ts:157
	// return this.purchases(purchaseKey).exists ? 1 : 0;
	frame_dig 0 // purchaseKey: byte[]
	dup
	len
	itob
	extract 6 2
	swap
	concat
	box_len
	swap
	pop
	bz *ternary0_false
	intc 0 // 1
	b *ternary0_end

*ternary0_false:
	intc 2 // 0

*ternary0_end:
	// set the subroutine return value
	frame_bury 0
	retsub

// delistSkill(uint64)void
*abi_route_delistSkill:
	// skillId: uint64
	txna ApplicationArgs 1
	btoi

	// execute delistSkill(uint64)void
	callsub delistSkill
	intc 0 // 1
	return

// delistSkill(skillId: uint64): void
//
// Delist a skill. Only the seller can delist their own skill.
delistSkill:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts\SkillMarketplace.algo.ts:164
	// assert(this.skills(skillId).exists, 'Skill not found')
	frame_dig -1 // skillId: uint64
	itob
	box_len
	swap
	pop

	// Skill not found
	assert

	// contracts\SkillMarketplace.algo.ts:165
	// skill = this.skills(skillId).value
	frame_dig -1 // skillId: uint64
	itob
	frame_bury 0 // storage key//skill

	// contracts\SkillMarketplace.algo.ts:166
	// assert(this.txn.sender === skill.seller || this.txn.sender === this.admin.value, 'Not authorized')
	txn Sender
	intc 7 //  headOffset
	intc 5 // 32
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	==
	dup
	bnz *skip_or0
	txn Sender
	bytec 1 //  "ad"
	app_global_get
	==
	||

*skip_or0:
	// Not authorized
	assert

	// contracts\SkillMarketplace.algo.ts:168
	// updatedSkill: SkillMetadata = {
	//       name: skill.name,
	//       description: skill.description,
	//       skillType: skill.skillType,
	//       version: skill.version,
	//       price: skill.price,
	//       seller: skill.seller,
	//       ipcsCid: skill.ipcsCid,
	//       soldCount: skill.soldCount,
	//       listedAt: skill.listedAt,
	//       active: 0,
	//     }
	intc 2 // 0
	intc 3 // 64
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	intc 3 //  headOffset
	intc 8 // 256
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 9 //  headOffset
	intc 4 // 16
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 10 //  headOffset
	intc 4 // 16
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 6 //  headOffset
	intc 1 // 8
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	itob
	concat
	intc 7 //  headOffset
	intc 5 // 32
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 11 //  headOffset
	intc 3 // 64
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	concat
	intc 12 //  headOffset
	intc 1 // 8
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	itob
	concat
	intc 13 //  headOffset
	intc 1 // 8
	frame_dig -1 // skillId: uint64
	itob
	cover 2
	box_extract
	btoi
	itob
	concat
	bytec 4 // 0x0000000000000000
	concat
	frame_bury 1 // updatedSkill: SkillMetadata

	// contracts\SkillMarketplace.algo.ts:180
	// this.skills(skillId).value = updatedSkill
	frame_dig -1 // skillId: uint64
	itob
	frame_dig 1 // updatedSkill: SkillMetadata
	box_put
	retsub

// setPlatformFee(uint64)void
*abi_route_setPlatformFee:
	// feeBps: uint64
	txna ApplicationArgs 1
	btoi

	// execute setPlatformFee(uint64)void
	callsub setPlatformFee
	intc 0 // 1
	return

// setPlatformFee(feeBps: uint64): void
//
// Update platform fee (admin only)
setPlatformFee:
	proto 1 0

	// contracts\SkillMarketplace.algo.ts:187
	// assert(this.txn.sender === this.admin.value, 'Admin only')
	txn Sender
	bytec 1 //  "ad"
	app_global_get
	==

	// Admin only
	assert

	// contracts\SkillMarketplace.algo.ts:188
	// assert(feeBps <= 1000, 'Max fee is 10%')
	frame_dig -1 // feeBps: uint64
	pushint 1000
	<=

	// Max fee is 10%
	assert

	// contracts\SkillMarketplace.algo.ts:189
	// this.platformFeeBps.value = feeBps
	bytec 3 //  "pf"
	frame_dig -1 // feeBps: uint64
	app_global_put
	retsub

*create_NoOp:
	pushbytes 0xb8447b36 // method "createApplication()void"
	txna ApplicationArgs 0
	match *abi_route_createApplication

	// this contract does not implement the given ABI method for create NoOp
	err

*call_NoOp:
	pushbytes 0x4bb66c56 // method "listSkill(pay,byte[64],byte[256],byte[16],byte[16],uint64,byte[64])uint64"
	pushbytes 0xbb5cac56 // method "buySkill(pay,uint64)void"
	pushbytes 0x992d7472 // method "hasAccess(uint64,address)uint64"
	pushbytes 0xb02a4e61 // method "delistSkill(uint64)void"
	pushbytes 0xa8079c08 // method "setPlatformFee(uint64)void"
	txna ApplicationArgs 0
	match *abi_route_listSkill *abi_route_buySkill *abi_route_hasAccess *abi_route_delistSkill *abi_route_setPlatformFee

	// this contract does not implement the given ABI method for call NoOp
	err